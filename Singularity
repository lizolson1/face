Bootstrap: docker
From: pytorch/pytorch:1.4-cuda10.1-cudnn7-devel


%post
  chmod 777 /
  chmod 777 /root
  chmod 777 -R /var
  mkdir /Pool2/
  mkdir /Pool2/users/
  mkdir /Pool2/users/lizolson/
  mkdir /etc/localtime/
  mkdir /usr/bin/nvidia-cuda-mps-server/
  mkdir /usr/bin/nvidia-cuda-mps-control/
  mkdir /usr/bin/nvidia-persistenced/
  mkdir /usr/bin/nvidia-debugdump/
  mkdir /usr/bin/nvidia-smi/
  chmod -R 777 /Pool2


